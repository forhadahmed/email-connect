# Local dependency installs are runtime artifacts, not source.
node_modules/

# Test and coverage outputs are reproducible and should not be tracked.
coverage/

# Common local editor and OS noise.
.DS_Store
